'use client'
import React, { useState } from 'react';
import IPCIDR from 'ip-cidr';

const IPAddressForm = () => {

//   const [ipArray, setIpArray] = useState([]);

//   const generateIPArray = (address) => {
//     const cidr = new IPCIDR(address);
//     return cidr.toArray();
//   };

  
//     const generatedIPs = generateIPArray("192.168.1.0/24");
//     setIpArray(generatedIPs);

    
  

//   return (
//     <div>
//       {ipArray.length > 0 && (
//         <div>
//           <h3>Generated IP Addresses:</h3>
//           <ul>
//             {/* {ipArray.map((ip, index) => (
//               <li key={index}>{ip}</li>
//             ))} */}
//           </ul>
//         </div>
//       )}
//     </div>
//   );
};

export default IPAddressForm;
